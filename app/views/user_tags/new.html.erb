<br>
<br>
<div class="container">
<br>
<div class = "container">
    <div class="d-flex justify-content-center">
      <div id="topic-tag-container">
        <h2>add your tags</h2>
        <br>

        <% @topics.each do |topic| %>
          <h4><%= topic.name %></h4>
          <br>
          <div data-topic-wrapper="<%= topic.name %>">
            <% topic.tags.each do |tag| %>
              <button class="tag-button topic-tag-button" data-element-id="<%= tag.id %>" data-element="tag"><%= tag.name %></button>
            <% end %>

            <div class="selected-tags" data-topic-tags="<%= topic.name %>">

            </div>
          </div>

          <br>
          <br>
        <% end %>

          <button class="button btn" id="btn-next"><strong>next</strong></button>
        <br>
      </div>
    </div>
    <br>
</div>
<br>
<br>
<br>
